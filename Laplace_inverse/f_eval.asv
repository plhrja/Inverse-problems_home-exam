% Evaluates the given characteristic function, that is
% 
% f(t) = 1 if 0<=t<=1, else f(t) = 0
%
% Author: RS

function ft = f_eval(t)
    
    if t < 1 && t > 0
        ft = 1;
    else
        ft = 0;
    end
end